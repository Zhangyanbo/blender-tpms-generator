schema_version = "1.0.0"

id = "tpms_generator"
version = "2.0.0"
name = "Gyroid Generator"
tagline = "Exact parametric Gyroid minimal surface as a clean quad mesh"
maintainer = "Yanbo Zhang <zhangybspm@gmail.com>"
type = "add-on"

tags = ["Mesh", "Geometry"]

blender_version_min = "4.2.0"

license = ["SPDX:MIT"]

# This add-on only relies on numpy (bundled with Blender) and the standard bpy API.
